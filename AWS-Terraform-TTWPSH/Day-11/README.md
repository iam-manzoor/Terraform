# Terraform Functions

### What is function?
- Makes our life easy by re-using the code
- No custom functions only in-build functions available

### String Function
- `upper` upper("hello")
- `lower` lower("HELLO")
- `trim` trim('Hello Scha"," ") # trims the last character if the character matched
- `replace` replace(hello world"," ","-")
- `substr` substr("hell",0,3)
- `collection`